# Design System Audit — "Playful Geometric" — EngFlow `frontend/` (audit-v7)
**Mode:** strictly READ-ONLY, no file modified. **Override applied:** Be Vietnam Pro is the only family (headings 800/900, body 400/500); any remaining Outfit/Plus Jakarta Sans reference = defect.
**Evidence:** `tailwind.config.js` 130L · `index.html` 44L · `src/assets/main.css` 53L · `design-system.css` 1660L · `app-layout.css` 612L · `app-logo.css` 53L · `router/index.js` 228L · 38 route components · 36 components · `lucide-vue-next@0.368.0` dist. Cascade order (`main.js:12-15`): main.css → design-system.css → app-logo.css → app-layout.css; unlayered CSS beats `@layer base`.
**Global totals:** 114 `<AppButton>` vs **38 raw `<button>` in views** · 96 `bg-white` · 33 `text-[Npx]` · 12 `shadow-[…]` · 28 `border-4` · 114 `transition-all` · 51 raw `max-w-*` · `py-3` 46× vs `py-24` 3× · `grid-cols-12` 0× · **0×** `animate-wiggle|animate-marquee|animate-float` · 104 `focus:ring-*` / 54 `focus:outline-none`.
## SECTION A — Token report (`item | spec | actual @ file:line | verdict`)
A1 family-everything | BVP only | `tailwind.config.js:47-55` (sans/heading/mono all BVP), `--geo-font` `design-system.css:39,100`; 16/16 `font-family` decls use the token, 0 literals | **MATCH**
A2 Outfit / Plus Jakarta Sans | 0 references | 0 functional anywhere in `frontend/**`; only historical comments `index.html:32` ("Plus Jakarta Sans … dead payload removed"), `design-system.css:512` (JetBrains Mono). No `<link>`, `@font-face`, or webfont dep left | **MATCH** (comments kept as documentation)
A3 loaded weights | 400/500/800/900 | `index.html:38` = `ital,wght@0,400;0,500;0,600;0,700;0,800;0,900;1,400` — 600 + italic-400 dead; `font-extrabold`(800) used **0×**, `font-semibold` 5× | **DEVIATION** → B-04
A4 delivery / font-display | self-host, `swap`, pinned subsets | Google **CDN** single `<link>`; `display=swap` ✓; 2× `preconnect` ✓; no `@fontsource` in `package.json`, no `public/fonts`, **no `&subset=`** (Vietnamese needs latin-ext) | **DEVIATION** → B-05
A5 palette 11 tokens | exact 11 hexes | `tailwind.config.js:11-24` and `design-system.css:12-29` both match spec hexes (`#FFFDF5 #1E293B #F1F5F9 #64748B #8B5CF6 #F472B6 #FBBF24 #34D399 #E2E8F0 #FFFFFF #8B5CF6`) | **MATCH** (but duplicated in 2 config blocks + 1 CSS block, same values, 3 owners)
A6 text on tertiary/quaternary | decorative only, never text on light | 6 violations on light bg: `Profile.vue:43`, `AdminDashboard.vue:19,20`, `LessonPreview.vue:31,59`, `TypingGame.vue:51,75` (+2 on dark sidebar `AdminLayout.vue:21,59` = acceptable) | **DEVIATION P1** → B-06
A7 radii 8/16/24/full | closed scale | `tailwind.config.js:68-71` + `design-system.css:54-57` ✓, but `extend` leaves Tailwind defaults alive: `rounded`(4px) 27×, `rounded-xl`(12px) 5×, `rounded-2xl` 1×, `rounded-none` 1×; 8 `blob-*` keys **0 usages** | **DEVIATION** → B-15
A8 pop shadows 4/6/2, card 8 | exact hex, mobile 2px | `tailwind.config.js:84-93` all MATCH spec hex; mobile reducer exists **only** as CSS patch `design-system.css:594-615` (`@media max-width:768px`) | **PARTIAL** → B-03
A9 off-scale shadow tiers | 2/4/6/8 | `--geo-shadow-sm: 3px` `design-system.css:61` (consumed by `.app-auth__icon-wrap:598`, `.app-hero__shape--tri:537`); navbar `0 4px 0 rgba(139,92,246,.35)` `app-layout.css:12`; admin active `3px 3px 0 rgba(0,0,0,.25)` `:400`; admin topbar `:444` | **DEVIATION** → B-33
A10 border-width 2px | default | `tailwind.config.js:97` ✓, `--geo-border-width` ✓; **`border-4`/`border-l-4`/`border-t-4` used 28× in 7 files** (violates A10) | **DEVIATION** → B-34
A11 icons | lucide sw 2.5, round caps, always in a colored shape | `.lucide{stroke-width:2.5}` `design-system.css:9` — **verified real**: `createLucideIcon.js:24` emits `class:["lucide","lucide-<name>"]`, so all 162 `<component :is="Icon">` sites render 2.5; round caps = library default (`defaultAttributes.js:16-17`) ✓. Enclosure **fails**: 4 admin `<SearchIcon class="absolute …">` + `Profile.vue:43 <Flame>` float unboxed; 7 raw SVGs use sw 2 / 3 | **MATCH/PARTIAL** → B-11
A12 motion tokens | bounce, dur 300, pop-in, reduced-motion | `bounce` ✓ `tailwind.config.js:98-100`, `--geo-transition: all 300ms bounce` ✓ (`design-system.css:81`), `pop-in` 0.5s ✓; **`wiggle` = `ease-in-out` not bounce** `:121`; keyframes duplicated (config `:101-120` vs CSS `:361-397`); reduced-motion guarded twice `:464-487`, `:1273-1287` | **DEVIATION** → B-25
A13 layouts | max-w-6xl, `py-24`, 12-col grouped | containers = **80rem = max-w-7xl** (`app-layout.css:16,242,507,541`, `Container.vue:19`); `py-24` 3× vs `py-3` 46×; `.geo-section` = `4rem/5rem` (`PageSection.vue:45-48`); `grid-cols-12` **0×**; no `scrollBehavior` in `router/index.js` | **DEVIATION** → B-27, B-34
A14 button spec | primary/secondary exact | `.app-btn` + `--primary`/`--secondary` (`design-system.css:916-963`) = accent/white/rounded-full/2px fg/4px→6px→2px + hover/active translate; secondary transparent→tertiary on hover | **MATCH — fully spec-compliant**
## SECTION B — Findings (`ID | P | file:line | problem | concrete fix`)
B-01 | P1 | design-system.css:154-268, 307-358, 400-461, 417-427 | Whole `geo-*` component layer (`.geo-btn*`, `.geo-input`, `.geo-label`, `.geo-card`, `.geo-badge*`, `.geo-squiggle`, `.geo-dot-grid`, `.geo-blob-*`) ships ~230 lines and is used by **zero** templates; `.app-*` duplicates it. Real `geo-*` usage is only `.geo-markdown`, `.geo-radio`, `.geo-audio`. Spec-exact input border `#CBD5E1` lives on the dead `.geo-input:311` while live `.app-input:1077` uses `#E2E8F0` | Delete the dead block; move the spec value onto the live rule: `.app-input { border: var(--geo-border-width) solid #CBD5E1; }` (`:1077`)
B-02 | P1 | AdminLayout.vue:4-75 + app-layout.css:271-489 | Admin shell is not responsive: permanent `w-72` (288px) sidebar inside `flex h-screen overflow-hidden`, no hamburger, no breakpoint → ~87px content at 375px, and `overflow-hidden` blocks even a scrollbar. The **compliant responsive implementation already exists and is 100% dead CSS** (`.app-admin`, `__overlay`, `__sidebar{translateX(-100%)}`, `--open`, `__hamburger`, 3 media rules) | Adopt it: root `class="app-admin"`; aside `class="app-admin__sidebar" :class="{'app-admin__sidebar--open': open}"`; `<button class="app-admin__hamburger" aria-label="Mở menu" aria-expanded="open">`; `<div v-if="open" class="app-admin__overlay" @click="open=false">`; `<main class="app-admin__main">`
B-03 | P1 | tailwind.config.js:83-94 + design-system.css:594-615 | Mobile pop reduction is a CSS patch over generated utilities, not a theme feature: (1) `hover:shadow-pop-*` (17 sites) and `active:shadow-pop-*` (3) are **absent from the override selector list** so they re-inflate on touch; (2) mobile `pop-xl`/`pop-featured` reduce to **3px**, off the 2/4/6/8 scale; (3) override repeats raw hexes instead of tokens | Make shadows var-driven: `boxShadow:{ pop:'var(--geo-pop)', 'pop-hover':'var(--geo-pop-hover)', 'pop-xl':'var(--geo-pop-xl)' }` + `:root{--geo-pop:4px 4px 0 #1E293B; --geo-pop-hover:6px 6px 0 #1E293B; --geo-pop-xl:8px 8px 0 #E2E8F0}` and `@media(max-width:768px){:root{--geo-pop:2px 2px 0 #1E293B;--geo-pop-hover:2px 2px 0 #1E293B;--geo-pop-xl:2px 2px 0 #E2E8F0}}`; meanwhile append `hover\:shadow-pop-\*,active\:shadow-pop-\*` to `:607` and change `3px`→`2px` at `:610-611`
B-04 | P1 | index.html:38 | Font URL loads faces the type spec doesn't own: `ital,wght@0,400;0,500;0,600;0,700;0,800;0,900;1,400`. 600 is used only 5× (`font-semibold`), italic-400 0×, while the spec's 800 tier is used **0×** (`font-extrabold` = 0) | `family=Be+Vietnam+Pro:wght@400;500;700;800;900&display=swap` (drop `ital` + 600), replace the 5 `font-semibold` with `font-bold`/`font-medium`, and write the 800 headings as `font-extrabold`
B-05 | P1 | index.html:36-38, package.json | Fonts are third-party-CDN only: no `@fontsource/*` dep, no `public/fonts`, no `@font-face`; every first paint depends on a `fonts.googleapis.com` round trip (blocked on offline/China-network/PII policy), no `size-adjust` metric fallback, and no subset pinning | Self-host: add `@fontsource-variable/be-vietnam-pro`, `import '@fontsource-variable/be-vietnam-pro'` in `main.js:12`, delete `index.html:36-38`, add `font-display:swap` (already in the variable file) and `.fallback{font-family:system-ui;size-adjust:104%}` for CLS
B-06 | P1 | Profile.vue:43, AdminDashboard.vue:19,20, LessonPreview.vue:31,59, TypingGame.vue:51,75 | Constitutional "tertiary/quaternary are decorative only, never text" rule broken: `text-tertiary` (amber) ≈ **1.6:1** and `text-quaternary` (emerald) ≈ **1.7:1** on `#FFFDF5` — effectively invisible | `Profile.vue:43` → wrap: `<span class="w-8 h-8 rounded-full bg-tertiary border-2 border-foreground inline-flex items-center justify-center"><Flame class="w-4 h-4 text-foreground"/></span>`; `AdminDashboard.vue:19,20` → `iconColor="text-foreground"` (the `bg-*/10` chip is already the decoration); `LessonPreview.vue:31,59` + `TypingGame.vue:51,75` → `text-success` (`#059669`, 4.8:1)
B-07 | P1 | Register.vue:50, DeckCreate.vue:74, AiVocabGenerator.vue:58,83, PremiumCheckout.vue:35, AdminExercises.vue:246,267, SearchVocabulary.vue:41,83 (+12 more) | `text-secondary` used 20× as the **error message** color (`role="alert"` in 5): pink `#F472B6` on cream ≈ **2.5:1**, fails AA. Errors are the least legible text in the app; `text-danger` (`#E11D48`, 5.6:1) exists but is used for other things | Global rule: every `role="alert"` / validation message = `text-danger`; those 5 sites → `font-bold text-xs uppercase tracking-wider text-danger text-center`. `SearchVocabulary.vue:41` decorative "?" → `text-foreground`
B-08 | P1 | design-system.css:358, 1449, LessonPreview.vue:26, AdminLessons.vue:243, AdminLessonBuilder.vue:398, SpeakingSkill.vue:12, FlashcardGame.vue:87,137,140 | `audit-v6 F25` fixed white-on-quaternary **only in the two button variants** (`:235-236`, `:1010-1011`); the same 2.1:1 pair survives in `.geo-badge-quaternary{color:white}`, `.app-avatar--quaternary` (no color rule at all), and 5 view sites; `bg-secondary text-white` (2.6:1) ×4 | `.geo-badge-quaternary{color:var(--geo-fg)}` (`:358`), add `.app-avatar--quaternary{color:var(--geo-fg)}` after `:1449`; `LessonPreview.vue:26` `'bg-quaternary text-white'`→`'bg-quaternary text-foreground'`; `AdminLessons.vue:243`, `AdminLessonBuilder.vue:398`, `SpeakingSkill.vue:12` `text-white`→`text-foreground`; `FlashcardGame.vue:87,137,140` likewise
B-09 | P1 | tailwind.config.js:113-118,123 + design-system.css:361-375 + (missing markup) | Marquee texture (spec §6, "keyword strips") is defined **twice** — Tailwind `marquee` keyframe + `animate-marquee`, and `.geo-marquee`/`-content` — and used **zero** times; `grep marquee|Marquee` in all `.vue` = 0 hits. Keyword strips render as static rows | Add and use the primitive (`Home.vue` after hero, `SearchVocabulary.vue` above results): `<div class="geo-marquee" aria-hidden="true"><div class="geo-marquee-content"><span v-for="k in keywords">{{k}}</span><span v-for="k in keywords">{{k}}</span></div></div>` — content must be duplicated for the `-50%` loop; if the signature is being dropped on purpose, delete both definitions instead
B-10 | P1 | Home.vue:47,82-85 + app-layout.css:539-554 | Feature-grid signature wrong twice over: spec = **3** cards joined by a **dashed SVG line** with alternating violet/pink/yellow headers; actual = **4** cards in `md:grid-cols-2 lg:grid-cols-4`, no connector. The compliant 3-up block (`.app-features__grid{repeat(3,1fr)}`, `__bar--accent/secondary/tertiary`) is dead because `Home.vue:47` reaches for Tailwind's grid | `Home.vue:47` → `<div class="app-features__grid relative">`, drop the 4th entry `:85`; connector as first child: `<svg class="hidden md:block absolute inset-x-0 top-[3.75rem] z-0" viewBox="0 0 800 8" preserveAspectRatio="none" aria-hidden="true"><path d="M40 4H760" stroke="var(--geo-fg)" stroke-width="2" stroke-dasharray="10 8" fill="none" stroke-linecap="round"/></svg>` + `relative z-10` on each card
B-11 | P1 | AdminExercises.vue:22, AdminLessonBuilder.vue:search, AdminUsers.vue:search, AdminSpeakingPrompts.vue:search, Profile.vue:43, Profile.vue:20 | Icon-enclosure rule (spec §4) violated in the 5 search inputs: `<SearchIcon class="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-muted-foreground">` — a bare icon floating on the field, no shape/border; `Profile.vue` mixes boxed (`:40` Zap) and unboxed (`:43` Flame) on one screen | `<span class="absolute left-2.5 top-1/2 -translate-y-1/2 w-7 h-7 rounded-full bg-accent border-2 border-foreground flex items-center justify-center"><SearchIcon class="w-4 h-4 text-white"/></span>` + bump the input's `pl-10`→`pl-12`
B-12 | P1 | AdminLessons.vue:41, AdminUsers.vue:37, AdminExercises.vue:53, AdminVideoLessons.vue:92, AdminLessonBuilder.vue:228, AppTable.vue:21 | 6 of 7 `<table>` have **no `overflow-x` wrapper** (only `AdminSpeakingPrompts.vue:173` does it right with `overflow-x-auto` + `min-w-[640px]`). Combined with B-02's 288px sidebar and `overflow-hidden` root, wide admin tables are unreachable — clipped, not scrollable | Wrap each: `<div class="overflow-x-auto -mx-4 px-4">…</div>` + `class="w-full min-w-[900px] text-sm"`; fix at the primitive too — add `overflow-x:auto` to `.app-table-wrap` (`design-system.css:1533`), which `AppTable.vue:20` already uses
B-13 | P2 | tailwind.config.js:10-24 vs :27-39 | Two complete color namespaces with identical values: `geo.*` (`bg-geo-bg`) and unprefixed aliases (`bg-background`). Both compile, both live (`bg-background` 22×, `bg-geo-bg` 2×), so no token has one owner and purging/refactors can break one side | Delete `:10-24` (the `geo.*` block), rewrite its 2 call sites (`AdminLayout.vue:4` `bg-geo-bg`→`bg-background`); keep `--geo-*` CSS vars as the runtime token source and the unprefixed aliases as the public utility API
B-14 | P2 | tailwind.config.js:43-45 + design-system.css:21-24 | `danger/warning/success` are outside the spec's 11 tokens yet used 76× combined (`text-danger` 28, `bg-danger` 25, `border-danger` 23). `--geo-danger-fg` exists in CSS but there is **no `danger-fg` key in Tailwind**, so any `text-danger-fg` silently compiles to nothing | Add `'danger-fg':'#FFFFFF'`, `'warning-fg':'#1E293B'`, `'success-fg':'#FFFFFF'` after `:45`; document the 3 semantic colors as an approved spec extension (removing them is not cheap)
B-15 | P2 | tailwind.config.js:67-82 (+34 markup sites) | `extend.borderRadius` cannot remove Tailwind defaults → 34 off-scale radii in markup (`rounded` 27×, `rounded-xl` 5×, `rounded-2xl` 1×, `rounded-none` 1×) sitting next to a spec scale of 8/16/24/full | Sweep markup (`rounded`→`rounded-sm`, `rounded-xl`→`rounded-md`, `FlashcardGame.vue:52` `rounded-2xl`→`rounded-lg`) then make it structural: move to `theme.borderRadius:{none:'0',sm:'8px',md:'16px',lg:'24px',full:'9999px'}` (no `extend`) and delete the 8 unused `blob-*` keys
B-16 | P2 | AdminLessonBuilder.vue:165,223, AdminVideoLessons.vue:57, AdminLessons.vue:164 | `font-mono` on 4 transcript/JSON textareas now resolves to Be Vietnam Pro (A1/BVP fold), so column alignment in pasted YouTube transcripts and JSON is gone while the class name still claims mono — the `audit-v5` fold traded one bug for another | Either rename the sites to `tracking-tight text-sm` (honest, zero risk) or re-add a loaded mono font as `fontFamily.mono` — the latter contradicts the BVP-everywhere override, so ask before shipping
B-17 | P2 | design-system.css:81,1414-1419 + Lessons.vue:124 + 11 sites | `--geo-transition: all 300ms bounce` on `.app-btn/.app-card/.app-input/.app-tabs__tab/.app-navbar__link` (114 `transition-all` in views) and 12 progress bars animate **`width`** (layout), incl. `Lessons.vue:124` = `transition-all duration-1000` on a per-card width change — a 1s reflow with a bounce curve | `.app-progress__bar{transition:transform .4s var(--geo-bounce);transform-origin:left}` + `style="transform:scaleX(var(--p))"`; for utilities `:style="{transform:'scaleX('+pct/100+')'}" class="origin-left transition-transform duration-300 ease-bounce"`; narrow the token to `transform 200ms cubic-bezier(.34,1.56,.64,1), box-shadow 200ms …, color 150ms linear`
B-18 | P2 | main.css:13-19 vs design-system.css:91-106 | `@layer base` re-declares the full `body` block (bg/color/font-family/smoothing/`line-height:1.6`) that `design-system.css` sets **unlayered**, so the main.css copy can never win and `line-height` differs (1.6 vs config's per-size LE); `scrollbar-gutter:stable` exists only in the winner. `html{scroll-behavior:smooth}` is also declared twice | Delete `main.css:13-19` and `main.css:5-9`; keep one owner per selector (`design-system.css:91-106`), leaving `main.css` as `@tailwind` + `*:focus-visible` + `.skip-link` only
B-19 | P2 | App.vue:64-77 | The `audit-v6 F32` "fix" injects `<link rel="stylesheet" href="/src/assets/main.css">` from a runtime `<style>` block. In a production build `/src/assets/main.css` is not an emitted path (Vite hashes it) → guaranteed 404 blocking request, and in dev it loads **every global rule a second time** from a different cascade origin, which can flip `.app-*` vs `--geo-*` specificity fights | Delete the `<link rel="stylesheet" href="/src/assets/main.css">` line and its comment; the CLS it targeted is already handled by `html{scrollbar-gutter:stable}` (`design-system.css:95`) + `min-height:96px` footer (`app-layout.css:201`)
B-20 | P2 | design-system.css:940, 265, 1188-1189, 1340-1341, 1585, 1450 | Tap targets below the 48px rule: `.app-btn--sm{min-height:44px}` used ~30× (retry buttons: `Decks.vue:41`, `FlashcardGame.vue:19`, `SpeakingList.vue:43`, `MemoryMatchGame.vue:20`, `TypingGame.vue:20`, `MixedGame.vue:17`, `SubmissionHistory.vue:22`), `.geo-btn-sm{36px}`, `.app-alert__close` 36×36, `.app-modal__close`/`.app-dropdown__trigger` 40px | `.app-btn--sm{padding:.5rem 1.125rem;font-size:var(--geo-text-xs);min-height:48px}` (`:940`); icon-only controls → `min-width/min-height:48px` at `:1188-1189`, `:1340-1341`, `:1585`
B-21 | P2 | Lessons.vue:46, SearchVocabulary.vue:16, AdminExercises.vue:173,180,191,204,229,234 + 82 | ~90 controls in `views` have neither `id` nor `aria-label` (manual `input|select|textarea` scan); placeholders are doing label duty — placeholder-as-label disappears on input and isn't reliably announced | Cheapest correct fix per site: `SearchVocabulary.vue:16` → `aria-label="Từ cần tra"`, `Lessons.vue:46` → `aria-label="Lọc bài học"`, `AdminExercises.vue:173` → wrap in `<FormField id label>`; rule: placeholder is never a label
B-22 | P2 | SquiggleDivider.vue:10-12 (+0 usages), design-system.css:456-461 (0 usages) | Squiggle texture is entirely absent from the rendered app, and the component is broken: `styleVars = {'--deco-squiggle-color':'color:var(--geo-fg)'}` is an invalid custom-property value, the template never consumes it, and the `color`/`height` props are ignored while the SVG stroke is hardcoded `%231E293B` | `const styleVars = { '--deco-squiggle-color': props.color, '--deco-height': props.height }` + `.squiggle-divider{stroke:var(--deco-squiggle-color)}` via `stroke='currentColor'`/`color:var(--deco-squiggle-color,var(--geo-fg))`; render `<SquiggleDivider />` under `Home.vue:11` and above `.app-cta:60`; delete `.geo-squiggle`
B-23 | P2 | DecoConfetti.vue:23-33,44-47 (0 usages) | Confetti primitives are dead **and** wrong: `size` implicitly sets brand color (`sm`=tertiary…`xl`=quaternary), and `const sizePx = sizeMap.md` is a constant so `size="sm|lg|xl"` renders 18px anyway | `const sizePx = computed(()=>sizeMap[props.size])`; make color explicit: `color` prop + `.decoconfetti--accent{color:var(--geo-accent)}`; then use it (`PremiumPage.vue:36` featured card, `MixedGame.vue` result). Delete if it stays unused
B-24 | P2 | components/ui (7 of 18 unused) + index.js:8-16 | `AppTabs, AppTable, AppStepper, AppProgress, AppAvatar, AppDropdown, AppTooltip` = **0 usages** (verified by `<Name[\s>/]` tag count) while views hand-roll the same things: `LessonLayout.vue:6-16` re-implements tabs, 6 admin files re-implement modals (`AdminExercises.vue:329`, `AdminLessonBuilder.vue:325`, `Decks.vue:99`, `AdminSpeakingPrompts.vue:243`, `AdminUsers.vue`, `AdminLessons.vue`), 6 hand-roll tables. `AppAlert/AppToast/AppSkeleton/AppErrorState` = 0 tag usages yet 61 raw-class usages (`class="app-alert …"`) — markup duplicating primitives. `index.js` exports only 9/18 | Import what exists: tabs → `<AppTabs>` (CSS already correct at `design-system.css:1364-1402`), 5 overlays → `<AppModal>` (`.app-modal*` at `:1294-1362`), tables → `<AppTable>` (+B-12 fix inside), 6 alert/toast markup sites → `<AppAlert>`/`<AppToast>`; add the 9 missing exports to `index.js`
B-25 | P2 | design-system.css:464-487, 1273-1287 | Reduced-motion guards reference **only dead classes** (`.geo-shadow-pop:hover`, `.geo-card:hover`, `.geo-btn-*:hover`) — the live surface (`.shadow-pop`, `hover:-translate-*` 49 sites, `hover:scale-110` `Lessons.vue:88`) is only duration-neutralised (`transition-duration:0.01ms`), so transforms still **jump**, and `html{scroll-behavior:smooth}` is never gated | Append to `:1287`: `.app-btn:hover, .app-card--interactive:hover, [class*="hover:-translate"]:hover, [class*="hover:scale"]:hover, [class*="hover:rotate"]:hover, .app-tabs__tab { transform:none !important; }` + `html{scroll-behavior:auto}` + `.geo-marquee-content{animation:none!important}`
B-26 | P2 | app-layout.css:497-506 + Home.vue:3-41,6,21 | The hero's "big amber circle behind text" signature does not render: `Home.vue` contains **no `.app-hero__bg`/`.app-hero__blob` element**, so all 4 blob rules are dead; the dot pattern sits on the text column (`Home.vue:7` `-z-10` inside `.app-hero__text`) instead of behind the image; right column is 3 abstract shapes, no image/blob-mask; and `.app-hero__text` + `.app-hero__stat` (`Home.vue:6,21`) have **no CSS rule anywhere** | Add `<div class="app-hero__bg" aria-hidden="true"><span class="app-hero__blob app-hero__blob--lg"/><span class="app-hero__blob app-hero__blob--md"/><span class="app-hero__blob app-hero__blob--sm"/></div>` as `Home.vue:4`'s first child; move `DotBackground` onto `.app-hero__visual` (`:35`); define `.app-hero__text{position:relative;z-index:1}` + `.app-hero__stat{min-width:5rem}` or delete both hooks
B-27 | P2 | Container.vue:19-27 + app-layout.css:16,242,507,541 | 3 container systems and none matches spec `max-w-6xl`: `.geo-container{80rem}` (=7xl, +`sm:40rem`/`xl:96rem`), 4 inline `max-width:80rem`, and 51 raw `max-w-*` in markup (`max-w-7xl` 10×, `max-w-6xl` only 4×). `Container.vue` is used 3×, `PageSection` 2× — the primitive exists and views ignore it | One owner: `.geo-container{max-width:72rem}` (=1152px spec) or amend spec to 7xl; replace the 4 inline `80rem` with `max-width:var(--geo-container-max)`; convert `max-w-7xl mx-auto px-4` sites to `<Container>`
B-28 | P2 | design-system.css:1175-1178, 1211-1214, 1230, 1265 | The alert/toast/state layer is built from 6 off-palette hexes (`#ECFDF5 #FFF1F2 #FFFBEB #F5F3FF #EAE4D6 #FFF1F2`) — greens/reds/ambers from Tailwind's default ramp, not the Playful palette, so feedback UI looks like a different design system | `background: color-mix(in srgb, var(--geo-success) 10%, var(--geo-bg))` (pattern already proven in `Pagination.vue:164,186`) for each of the 10 sites; `#EAE4D6` shimmer → `color-mix(in srgb, var(--geo-tertiary) 12%, var(--geo-muted))`
B-29 | P2 | AppButton.vue:74, FormField.vue:24,29, AppToast.vue:23, AppDropdown.vue:33, AppTooltip.vue:16, AppEmptyState.vue:10, Header.vue:14, AppAvatar, AppProgress.vue:19 | 16 class names applied in templates have **no rule anywhere** (checked against 807 defined selectors incl. every scoped `<style>`): `app-btn__label`, `app-btn--loading`, `app-btn--md`, `app-form-field`, `app-form-field__control`, `app-form-field--invalid`, `app-toast__body`, `app-dropdown__trigger`, `app-tooltip__trigger`, `app-state--empty`, `app-empty-title`, `app-hero__text`, `app-hero__stat`, `app-navbar__lock`, `app-tabs__panel`, `app-avatar__img`. 4 primitives build names by interpolation (`'app-alert--'+variant`) so a missing variant is silent. `AppEmptyState.vue:10` also hardcodes `id="app-empty-title"` referenced by nothing (duplicate-id if two render) | Write the 3 that matter and delete the rest: `.app-form-field{display:flex;flex-direction:column} .app-form-field__control{position:relative} .app-form-field--invalid .app-input{border-color:var(--geo-danger)}` `.app-btn--loading{opacity:.7;pointer-events:none}` `.app-navbar__lock{color:var(--geo-secondary);margin-left:.25rem}`; switch interpolated names to explicit lookup maps (as `AppButton` variants already are); remove or wire the id (`aria-labelledby`)
B-30 | P2 | AdminVideoLessons.vue:30,38,51,57, AdminSpeakingPrompts.vue:67,82,119, AdminExercises.vue:27,37 + all textareas | `select` and `textarea` have **no primitive** (`AppInput` = `input` only), so 5 selects + 4 textareas hand-roll `border-2 border-foreground bg-white rounded-md font-bold … focus:ring-2` in 8 slightly different recipes and never get `.app-input`'s spec focus shadow (`4px 4px 0 accent`) | Add `components/ui/AppSelect.vue` (`class="app-input appearance-none pr-10"` + a `ChevronDown` inside a `.app-features__icon`-style chip) and `AppTextarea.vue` (`.app-input` + `min-height:6rem;resize-y`); swap the 9 sites
B-31 | P2 | views/** (38 raw `<button>`) + AdminLayout.vue:113-121,63 | 38 raw `<button>` in views with custom class strings (worst: `AdminExercises.vue` 4/4, `VideoLesson.vue` 3, `ListeningGame.vue` 2, `Decks.vue` 2, `SearchVocabulary.vue`, `DeckDetail.vue`, `FlashcardGame.vue`, `MixedGame.vue`, `QuizGame.vue`, `MemoryMatchGame.vue`, `LessonExerciseTab.vue`, `MatchingExercise.vue`, `AiGeneratePanel.vue`, `VideoLibrary.vue`, `AdminLessonBuilder.vue`, `AdminLessons.vue`, `AdminSpeakingPrompts.vue`, `AdminVideoAttempts.vue`) — each repeats `border-2 … shadow-pop-sm rounded-md`, so any shadow/radius fix needs 38 edits; `AdminLayout.vue:113-121` stores 8 whole Tailwind class strings in a JS array (`activeClass`), which Tailwind purging and the mobile reducer both struggle to reach | Convert actions to `<AppButton>`; for the legitimate absolute-positioned icon chips (`FlashcardGame.vue:72` — already has `aria-label`, good) extract `<AppIconButton>` into `components/ui/`; move `AdminLayout.vue` active strings into a CSS class (`.admin-nav-link--active`)
B-32 | P3 | 96× (worst: AdminLessonBuilder.vue 27, AdminExercises.vue 23, AdminLessons.vue 10, AdminSpeakingSubmissions.vue 7, AdminSpeakingPrompts.vue 4, AiGeneratePanel.vue 4, PremiumPage.vue 3, Lessons.vue 3) + `Lessons.vue:219-223` + ~30 rgba sites | Token bypass by literal: `bg-white` instead of `bg-card` 96×; `levelColor()` returns `var(--geo-*, #hex)` with **off-palette fallbacks**; ~30 `rgba(...)` copies of token colors (`app-layout.css:12,78,167,217,267,282,389,394,444,480`, `design-system.css:713,765`, `LearningPath.vue:334`, `AppTable` overlays) | `bg-white`→`bg-card` (identical value, now token-owned); drop every `var()` fallback hex; add `--geo-accent-04/-08/-10/-35`, `--geo-tertiary-25`, `--geo-fg-55` alpha vars in `:root` and reference them instead of rgba
B-33 | P3 | app-logo.css:23, main.css:29, design-system.css:520,577, app-layout.css:467,577,535,559, `--geo-shadow-sm:61`, AdminLayout.vue:117,120 + 6 more | Off-scale literals: `border-radius:4px` ×3 + `6px` ×1, `2rem 2rem 2rem 0` ×2 (blob written by hand while 8 `blob-*` tokens sit unused), `--geo-shadow-sm:3px`, and 12 `shadow-[…]` arbitrary shadows (`AdminLayout.vue:117,120` = `shadow-[4px_4px_0px_0px_white]`) | Radii → `var(--geo-radius-sm)` or add `--geo-radius-xs:4px` as a real token; blobs → `border-radius: var(--geo-radius-blob)` using `tailwind.config.js:73`; `shadow-[…]` → `shadow-pop`/`shadow-pop-white` token added to `boxShadow`
B-34 | P3 | 28 sites in 7 files (AdminLessonBuilder.vue 8, SpeakingRecord.vue 5, VideoLesson.vue 4, AdminDashboard.vue 3, SpeakingDetail.vue 3, AdminSpeakingSubmissions.vue 3, SubmissionHistory.vue 2) + all views | Border-width drift: `border-4`/`border-l-4`/`border-t-4` break the 2px system; vertical-rhythm drift: spec `py-24` appears 3× while `py-3` (12px) appears 46×, `py-2` 15×, `py-32` 4×, and `.geo-section` invents a 4rem/5rem tier | Type-block accents → `border-l-2` (+ keep the color) or promote a documented `--geo-accent-bar:4px` exception; paddings → `py-24` for page-level sections and one `--geo-section-y` var replacing `.geo-section`'s custom numbers
B-35 | P3 | 33 sites in 17 files (AdminLessonBuilder.vue 5, VideoLesson.vue 4, Lessons.vue 3, Profile.vue 3, SearchVocabulary.vue 2, Decks.vue 2, ListeningGame.vue 2, MemoryMatchGame.vue 2 …) | `text-[Npx]` arbitrary sizes (10/11/12/13/15/16/22px) bypass the Major-Third scale defined twice already (`tailwind.config.js:54-66` + `--geo-text-*` `design-system.css:42-51`) | `text-[10px]`/`text-[11px]` → add `2xs:['0.625rem',{lineHeight:'1'}]` **or** use `text-xs`; `text-[13px]`→`text-sm`, `text-[15px]`→`text-base`, `text-[22px]`→`text-2xl`; add a one-line note that the two scale copies must stay in sync
B-36 | P3 | VideoLesson.vue:106,108,109,111, SearchVocabulary.vue:78,85, PremiumCheckout.vue:35, TypingGame.vue, SpeakingSkill.vue:12, AdminLessonBuilder.vue:63 (`bg-tertiary/10 text-foreground`), AdminDashboard donut | Glyph-as-text icons (`🔁 🎤 ● ■`) — unstyled, not lucide, announced as noise; 3 badges use 1px 15-20% alpha borders (`border-foreground/15`, `border-secondary/20`) instead of 2px solid; **live bug**: `AdminDashboard.vue:106-109,130` builds colors from `hsl(var(--accent))`/`hsl(var(--foreground))` but only `--geo-*` exist → the donut renders black and 4 chart segments invalid | Emoji → lucide in a chip: `<span class="w-6 h-6 rounded-full bg-foreground/10 border-2 border-foreground inline-flex"><Repeat class="w-3.5 h-3.5"/></span>` (`Repeat/Mic/Circle/Square` all exist); badges → `border-2 border-foreground`; `AdminDashboard.vue` → `var(--geo-accent)` / `conic-gradient(var(--geo-accent) 0 ${r}%, var(--geo-muted) ${r}% 100%)`
B-37 | P3 | AdminExercises.vue:237, AdminLessonBuilder.vue:188, all `<img>`, AdminLessonBuilder.vue:66/17/95/116/318, Lessons.vue:92,93 | 2 `<img>` with **no `alt`** (both admin image previews) and 0 of 6 audited `<img>` carry `width`/`height` → CLS; 7 raw inline SVGs off the 2.5 stroke (sw 2 ×1, sw 3 ×6); `Lessons.vue:92` `stroke="var(--geo-border, #E5DECF)"` invents an off-palette fallback | `:237` → `alt="Ảnh minh họa"`, `:188` → `alt=""` (decorative preview); add `class="w-full aspect-video"` or explicit `width/height`; set sw-2 site to `2.5`; `Lessons.vue:92` → `stroke="var(--geo-border)"`
B-38 | P3 | index.html:5, AdminLayout.vue:63, AdminLayout.vue:113-121, FlashcardGame.vue:51-53, AdminExercises.vue:215-220, App.vue:13, main.css:32-52 | Hygiene: favicon is a third-party dicebear image while the app owns a spec-conformant mark (`app-logo.css:19-44`); `AdminLayout.vue:63` logout has `active:translate` but **no hover, no shadow**, and `hover:bg-secondary hover:text-white` (2.6:1); sidebar nav active styles live as 8 inline Tailwind strings (`:117,120`); keyboard traps: `FlashcardGame.vue:51` `<div @click="flipCard">` (no role/tabindex/key handler — users cannot flip a card without a mouse) and `AdminExercises.vue:215` `<li @click>` with no `role="option"`/`listbox`; `.skip-link` animates `left/top` via `transition:all`; `LessonLayout.vue` panels use `aria-label` instead of `aria-labelledby` | `public/favicon.svg` from the logo shapes + `<link rel="icon" href="/favicon.svg">`; logout → `hover:-translate-y-0.5 hover:shadow-[3px_3px_0_rgba(0,0,0,.35)] hover:bg-white hover:text-foreground`; `FlashcardGame.vue:51` add `role="button" tabindex="0" :aria-pressed="isFlipped" @keydown.enter.prevent="flipCard" @keydown.space.prevent="flipCard"`; `:215` `<ul role="listbox">` + `<li role="option" tabindex="-1" :aria-selected>`; skip-link → `clip-path:inset(50%)`/`:focus{clip-path:none}` and drop `transition:all`; tabs: `:id="'tab-'+tab.id"` + panels `:aria-labelledby="'tab-'+tab.id" tabindex="0"`
## SECTION C — Component primitives inventory
C1 `src/components/ui/` (18): **AppButton** (114 uses; all 9 in-use variants have CSS rules — primary 28, secondary 45, tertiary 13, pink 7, danger 4, emerald 4, featured 3, amber 2, ghost 2; docblock `:4` lists only 5; **no `inheritAttrs:false`** while `useAttrs()` is also spread at `:44` → every aria/class bound twice) · **AppInput** 10 · **FormField** 10 (`:for`/`aria-describedby` wiring correct) · **StickerCard** 3 · **AppModal** 3 · **AppEmptyState** 2 · **AppAlert, AppToast, AppSkeleton, AppErrorState, AppTabs, AppTable, AppStepper, AppProgress, AppAvatar, AppDropdown, AppTooltip** = **0 tag uses** (alert/toast/skeleton/state instead appear as 61 raw `class="app-*"` sites: `ForgotPassword.vue:24-28`, `Login.vue:36`, `ResetPassword.vue:34-38`, `Decks.vue:19,41`, `AdminSpeakingSubmissions.vue:56-73`, `AdminSpeakingPrompts.vue:143-160`). `index.js` exports 9/18.
C2 `src/components/layout/`: Container (3), PageSection (2, +3 dead `DecoShape` at `:4-6`), PageHeader (1), StatCard (4). `src/components/decor/`: DotBackground (1 = `Home.vue:7`), DecoShape (3), SquiggleDivider (0), DecoConfetti (0). `common/`: Header (1), Footer (1), AppLogo (2), Pagination (12), UserPageHeader (**19** — the de-facto page header), GuestCtaCard (2), StreakCalendar (1). `home/LearningPath` (1), `lessons/MatchingExercise` (1), `admin/AiGeneratePanel` (1).
C3 Raw `<button>` **38 in `src/views`** (19 files, list in B-31) + 3 in components; raw `<input|select|textarea>` in views ≈ 120 of which ~90 lack id/aria-label (B-21). No icon-only raw button is missing `aria-label` (regex sweep of 700-char button bodies → 0 hits), which is a genuine strength.
C4 **Three parallel button systems:** `<AppButton>` 114 (`.app-btn--*`), raw `class="app-btn app-btn--*"` **2** (`Decks.vue:11,12`), `.geo-btn*` **0**, plus 38 hand-classed raw buttons. Three shadow systems: `shadow-pop-*` utilities, `--geo-shadow-*` vars, `shadow-[…]` literals. Three input systems: `.app-input` (10), raw `border-2 border-foreground rounded-md bg-white` (≈70), dead `.geo-input`.
## SECTION D — Per-view compliance matrix (37 routes, none skipped)
Legend — **shadow**: `✓`=pop utilities present & token-based · `~`=present but with `shadow-[…]` literals · `✗`=none found · **b2**: `✓` `border-2` used · **rad**: `✗`=off-scale `rounded/rounded-xl/2xl/none` present · **font**: `✗`=off-spec (`font-mono`/`font-semibold`) · **deco**: `–`=no dot/squiggle/marquee/blob/Deco shape · `T`/`S`/`C`/`B`=DotBackground/Squiggle/Confetti/blob · **a11y**: risk count (B-06/07/08/11/21/37/38 sites) · **motion**: `–`=no animate/transition hook · `⚠`=transitioning `width`
/ | shadow ✓(~0) | b2 ✓ | rad ✓ | font ✓ | deco T(broken) | a11y low (1 h1 ✓, `app-hero__text`/`__stat` no-op) | motion ⚠ | A8/B-26/B-10: no amber circle renders, 4-up grid, dots behind text not image
/login | shadow ✗ | b2 ✓ | rad ✓ | font ✓ | deco – (uses dead `.app-auth__*`… **CSS-defined, live**) | a11y med (B-07 error pink, `:31` checkbox 16px) | motion ✓ (`geo-pop-in` icon wrap) | all styling via `app-auth` classes; no `shadow-pop`, no `animate-*`
/register | shadow ✓ | b2 ✓ | rad ✓ | font ✓ | deco – | a11y med (B-07 `:50`) | motion ✓ (`animate-pop-in:11`) | 1 h1 ✓; icon chip `bg-secondary rounded-full` ✓ (enclosed icon pattern)
/forgot-password | shadow ✗ | b2 – | rad ✓ | font ✓ | deco – | a11y low (`role="alert"/"status"` ✓, FormField ✓) | motion ✓ (`geo-pop-in` via `.app-auth__icon-wrap`) | **zero `border-2`/`shadow-pop` in markup** — signature comes entirely from `app-layout.css`; `<h2>` only, no `<h1>`
/reset-password | shadow ✗ | b2 – | rad ✓ | font ✓ | deco – | a11y low (alert/status ✓) | motion ✓ (`.app-auth__icon-wrap`) | same as ForgotPassword; 1 `<h2>`; no decorative primitives in markup
/lessons | shadow ✓ | b2 ✓ | rad ✗(`rounded`×4) | font ✓ | deco B(dead `blob` refs only) | a11y med (`:46` unlabelled input, `:87` img w/o dims) | motion ⚠(`:124` width 1000ms, `:88` grayscale 700ms) | 6 off-token hexes + `levelColor` fallbacks (B-32); 3 `text-[Npx]`; only place `stroke-dasharray` appears (`:95`, a progress ring, not a connector)
/lessons/:id (`lessons/LessonLayout.vue`) | shadow ✗ | b2 ✓ | rad ✓ | font ✓ | deco – | a11y low-med (tabs ✓ `role=tablist/tab/tabpanel` + arrow keys; panels use `aria-label` not `aria-labelledby` B-38; **no `<h1>`**) | motion – | tabs hand-rolled (`:6-16`) while `AppTabs` sits unused; 4 `aria-*` attrs is the app's best tab implementation
/videos | shadow ✓ | b2 ✓ | rad ✗ | font ✓ | deco – | a11y med (`:63` `<img>`; 1 unlabelled control) | motion ✓ | 1 h1 ✓; `levelColor`-style inline `:style` backgrounds ×2; 1 `text-[Npx]`
/videos/:id (`VideoLesson`) | shadow ✓ | b2 ✓ | rad ✗ | font ✓ | deco B(inline blob classes) | a11y med (emoji buttons B-36; 4 unlabelled) | motion ✓ | heaviest view (593L): 4 `text-[Npx]`, 2 `border-2 … rounded-*` recipes, 12 raw `text-base`, 4 emoji icons, `:12` width transition
/profile | shadow ✓ | b2 ✓ | rad ✗(`rounded-none`) | font ✓ | deco – | a11y med (**B-06 `:43` text-tertiary**, `:20` `text-secondary` on light) | motion ⚠(`:37` width) | **zero `<h1>`** (uses `<h2>`×n); 3 `text-[Npx]`; `StreakCalendar` 1px grid borders (`:336`)
/search (`SearchVocabulary`) | shadow ✓ | b2 ✓ | rad ✗ | font ✓ | deco B | a11y med (`:16` unlabelled input; `:19` `aria-label` ✓ good) | motion ✓ (`ease-bounce` on input) | 1 `<h1>` via `UserPageHeader`; `shadow-[4px_4px_0px_0px_transparent]` literal `:17`; 2 `text-[10px]`; pink badge borders `:78,85`
/leaderboard | shadow ✓ | b2 ✓ | rad ✗(`rounded-xl`) | font ✓ | deco – | a11y med (rank `<td>` colour cells) | motion – | uses `text-danger`/`bg-danger` (28/25× app-wide); 1 `text-[Npx]`
/decks | shadow ✗ | b2 ✓ | rad ✓ | font ✓ | deco – | a11y low (3 aria ✓) | motion – | only view with **raw `.app-btn app-btn--*` markup** (`:11,12`) + `app-btn--md` which has no rule; 2 `text-[Npx]`; hand-rolled modal `:99`
/decks/:id (`DeckDetail`) | shadow ✓(17) | b2 ✓ | rad ✓ | font ✓ | deco – | a11y low | motion – | most shadow-dense user view; 1 raw `<button>`
/decks/:id/play/flashcard | shadow ✓ | b2 ✓ | rad ✗(`rounded-2xl`) | font ✓ | deco – | a11y high (**B-38 unflippable by keyboard `:51`**, `:87` `text-white` on quaternary B-08) | motion ✓ | 4 off-token hexes (`:137-146` colour map); `v-html="sanitizeText()"` on word/meaning/example — XSS surface depends entirely on `DOMPurify` usage
/decks/:id/play/quiz | shadow ✓ | b2 ✓ | rad ✓ | font ✓ | deco B | a11y med | motion ✓ | 1 raw `<button>`; 1 `rounded-lg` (=24px ✓)
/decks/:id/play/memory | shadow ✓ | b2 ✓ | rad ✓ | font ✓ | deco – | a11y med (`:106` hand-rolled `grid-template-columns:minmax(0,3fr)` layout) | motion ✓ (`animate-pop-in:39`) | 2 `text-[Npx]`
/decks/:id/play/typing | shadow ✓ | b2 ✓ | rad ✓ | font ✓ | deco – | a11y med (**B-06 `:51,75` text-quaternary**) | motion ✓ | `shadow-[…]` literal; 1 `text-[Npx]`
/decks/:id/play/listening | shadow ✓ | b2 ✓ | rad ✓ | font ✓ | deco – | a11y med (audio w/o transcript fallback) | motion ✓ (`animate-pop-in:16`) | 2 `text-[10px]`; `w-20 h-20` icon circle ✓ enclosed
/decks/:id/play/mixed | shadow ✓ | b2 ✓ | rad ✓ | font ✓ | deco – | a11y low | motion ✓ | 1 raw `<button>`; result state has no confetti (B-23)
/ai-vocab-generator | shadow ✓ | b2 ✓ | rad ✓ | font ✓ | deco – | a11y med (**B-07 `:58,83`**, 3 aria ✓) | motion ⚠(`:30` width) | 11 `border-2`; 3 arbitrary sizes; uses `AppTextarea`-less raw `<textarea>`
/decks/create | shadow ✓ | b2 ✓ | rad ✓ | font ✓ | deco – | a11y med (**B-07 `:74`**, `:70` 20px checkbox) | motion – | `shadow-[…]` literal; 2 `text-[Npx]`; raw `.app-input` markup ×4 (fine)
/speaking | shadow ✓ | b2 ✓ | rad ✓ | font ✓ | deco – | a11y low (retry ids ✓ — good pattern) | motion – | 1 raw `<button>`; 2 `text-[Npx]`
/speaking/history (`SubmissionHistory`) | shadow ✓ | b2 ✓ | rad ✓ | font ✓ | deco – | a11y low-med | motion – | 3× `border-4`; 2 `text-[Npx]`; hand-rolled `.app-skeleton` markup ×14
/speaking/:id (`SpeakingDetail`) | shadow ✓ | b2 ✓ | rad ✓ | font ✓ | deco – | a11y low (4 aria ✓) | motion – | 3 `border-4`; 1 `text-[Npx]`; `bg-white` ×1
/speaking/:id/record | shadow ✓ | b2 ✓ | rad ✓ | font ✓ | deco B(5) | a11y low (5 aria-label + ids ✓ best in app) | motion – | 5 `border-4`; the most decorated speaking view; mic/permission copy present
/premium | shadow ✓(1) | b2 ✓ | rad ✓ | font ✓ | deco – | a11y med (3 `bg-white` not `bg-card`; no focusable price cards) | motion – | **`variant="tertiary"` on both CTAs = pink**, spec says primary accent (`app-btn--tertiary` `:965-966`); featured badge `:37` `bg-tertiary text-foreground` ✓ (correct decorative use)
/premium/checkout | shadow ✓ | b2 ✓ | rad ✗(`rounded-xl`×2) | font ✓ | deco – | a11y med (**B-07 `:25,35`**) | motion – | 2 off-scale radii; `border-2 border-quaternary` ✓ decorative-only use done right
/admin (layout `AdminLayout.vue`) | shadow ~(8 `shadow-[…]`) | b2 ✓ | rad ✓ | font ✓ | deco B | a11y med (B-38 logout; 0 `aria-label`; `alt="admin"` generic) | motion – | **B-02 unbreakable sidebar**; 17 `border-2`; 11 `bg-foreground` overrides `--geo-fg` on its own border (invisible hairline); 8 arbitrary shadows
/admin/dashboard | shadow ✓ | b2 ✓ | rad ✓ | font ✓ | deco – | a11y med (B-06 `:19,20`; donut black — B-36) | motion ⚠(`:39` width) | 3 `border-4`; no `<h1>`; `hsl(var(--accent))` bug
/admin/lessons | shadow ✓(12) | b2 ✓(23) | rad ✓ | font ✓ | deco – | a11y med (B-08 `:243`; B-12 unguarded table) | motion – | no `<h1>`; 10 `bg-white`; 1 `text-[Npx]`; hand-rolled modal
/admin/exercises | shadow ✓(12) | b2 ✓(28) | rad ✓ | font ✓ | deco – | a11y high (**B-11 floating SearchIcon**, 6 unlabelled controls, `:215` li-click, `:237` no-alt img, B-07 `:246,267`) | motion – | 23 `bg-white` + 11 arbitrary sizes = worst token drift; `:97` difficulty chip map (`bg-tertiary text-foreground` ✓, `bg-secondary text-foreground` ✓ after B-08)
/admin/users | shadow ✓ | b2 ✓ | rad ✓ | font ✓ | deco – | a11y med (B-11 icon, B-12 table, `:67` img no dims) | motion – | 1 `text-[Npx]`; role badges use `bg-tertiary` ✓
/admin/speaking-prompts | shadow ✓ | b2 ✓ | rad ✓ | font ✓ | deco – | a11y low-med (table guarded ✓, 4 `app-input` selects unstyled-focus) | motion – | the **only** table with `overflow-x-auto` + `min-w-[640px]` (B-12 reference implementation); 2 `text-[Npx]`
/admin/speaking-submissions | shadow ✓ | b2 ✓(15) | rad ✓ | font ✓ | deco – | a11y med (14 raw `.app-skeleton` divs, B-36 `bg-tertiary/10`) | motion – | 3 `border-4`; hand-rolled overlay modal
/admin/videos (`AdminVideoLessons`) | shadow ✓ | b2 ✓ | rad ✓ | font ✗(`font-mono`) | deco – | a11y med (B-12 unguarded table, 4 unlabelled) | motion – | 1 `bg-white`; 5 `app-input` sites; the only admin view without an arbitrary text size
/admin/video-attempts | shadow ✓ | b2 ✓ | rad ✓ | font ✓ | deco – | a11y med (B-12, 2 `text-[Npx]`) | motion – | 1 `shadow-[…]`; grade buttons use `variant="tertiary"` (pink) for a primary action ×2
/admin/:id/build (`AdminLessonBuilder`) | shadow ✓(17) | b2 ✓(33) | rad ✗ | font ✗(`font-mono`×2) | deco – | a11y high (B-08 `:398`, `:188` no-alt img, B-11 icon, B-12 table, sw-2/sw-3 raw SVGs) | motion – | 27 `bg-white`, 8 `border-4`, 5 arbitrary sizes, 6 arbitrary shadows, hand-rolled delete overlay `:325` — highest total drift in the app
## SECTION E — Not verifiable from code
E1 **Actual rendered font.** Every family declaration is correct but a single missing `font-variation-settings`/`font-weight` mapping, or the 404 stylesheet (B-19) loading in dev, could still render 700 where 800 is required — needs a real-browser `getComputedStyle` pass per view, especially `Home`/`PremiumPage`/admin.
E2 **Real contrast + AA/AAA.** All ratios quoted in B-06/07/08 are computed from hex maths, not from rendered pixels: `opacity:.15` on the hero blobs, `/10`–`/35` alpha chips, and the `grayscale` card image (`Lessons.vue:88`) all change effective contrast; a DevTools contrast audit per view is required.
E3 **Mobile pop-shadow behaviour at 375px.** The reducer is a `max-width:768px` CSS block layered over Tailwind utilities — its actual computed result depends on class-output order (`hover:shadow-pop-lg` vs the media rule's specificity), so only a live device/emulator measurement can confirm spec compliance (B-03).
E4 **Horizontal scroll / CLS at 375px.** Needs a browser pass: `.app-hero__visual{height:28rem}` + 16rem/14rem absolute shapes inside `overflow:hidden`, `PageSection`'s `-top-16 -left-16` `DecoShape` **without** an `overflow-hidden` ancestor, `scrollbar-gutter:stable`, and the async-mounted Header/Footer are all CLS/overflow candidates.
E5 **Motion feel.** Whether 300ms bounce reads as "playful" or as lag, whether the missing `wiggle`/marquee/squiggle signatures are noticed at all, and whether the `duration-1000` width transition feels broken — perceptual, requires interaction.
E6 **Font loading with Vietnamese diacritics.** `latin` vs `latin-ext` subsetting (A4) and the FOUT window from `display=swap` (B-05) can only be seen on a throttled cold load; the `AppTable.vue:43` `"No data"` and `AppStepper.vue:31` `✓` glyphs also need a visual check under BVP.
E7 **Icons in a colored shape at small sizes.** 162 icons resolve their stroke from `.lucide`, but whether 2.5 looks right at `w-4 h-4`/`w-3.5` (and whether the `absoluteStrokeWidth` scaling in newer lucide versions will change this) needs rendering, not reading.
E8 **Print + forced-colors + zoom.** `@media print` (`design-system.css:836-909`) uses `#000/#f5f5f5/#e5e5e5/#ccc/#f0f0f0/#333` — deliberately greyscale, but unverified; and no `forced-colors`/`prefers-contrast` handling exists anywhere, so Windows High Contrast could drop the pop shadows entirely.
