# audit-v13 — T6.5: đóng/gia hạn các hạng mục OPEN từ audit-v12 bằng ĐO MỚI

Mỗi mục dưới đây được **đo lại trong phiên v13**, không kế thừa kết luận v12.

| # | Hạng mục v12 | Phép đo v13 | Kết quả | Trạng thái |
|---|---|---|---|---|
| **F147** | `POST /api/vocabulary` cho user thường ghi vào bảng vocabulary **toàn cục** (IDOR/scoping) | `POST /api/vocabulary` với token student, body hợp lệ, **không** `deckId` | **HTTP 400** `"Cần chọn bộ từ để lưu từ vựng. Từ vựng dùng chung chỉ quản trị viên mới thêm được."`; `F147_PROBE_ROWS=0` (không rò hàng) | **FIXED — xác nhận** |
| **F148** | Hai thuật toán SRS ghi cùng bảng; `/api/flashcards/review` dùng `isKnown` | `POST /api/flashcards/review` với payload cũ `{vocabularyId, isKnown}` | **HTTP 400** (schema `quality` 0–5 là bắt buộc) | **FIXED — xác nhận** |
| **F153** | Giảm flashcard còn 2 nút có thể phá streak | E2E T4.2b: submit bài → `study_days` có ngày hôm nay | `STUDY_DAYS_TODAY=1` | **FIXED — xác nhận** |
| **F150** | CLS 0.104 trên `/` | Lighthouse + CLS qua Playwright (Phase 5) | *chờ perf sweep* | ⏳ |
| **C6** | Azure Speech SDK ~15MB không dùng | `grep -rn "azure" pom.xml src/main` | *đo ở dưới* | ⏳ |

## Ghi chú

- F147/F148/F153 được xác nhận **bằng HTTP thật** trên container đang chạy, không phải bằng đọc code.
- Các mục này là "OPEN" ở v12 nhưng v12 Phase 9–11 đã sửa; v13 **đo lại** để xác nhận đúng (không giả vờ phát hiện lại).
- Không có hạng mục nào trong số này trở thành finding mới của v13.
