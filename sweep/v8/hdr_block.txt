/* audit-v8: the eight desktop nav links need ~1400px but the navbar inner box is
   capped at 80rem (1280px) and the links cannot shrink (white-space: nowrap), so
   on any laptop-width screen the header pushed past the viewport and produced a
   horizontal scrollbar. Compact the link padding for the 1280-1535 band only;
   above 1536 the nav has room and keeps its normal size, below 1280 the hamburger
   menu already takes over. */
@media (min-width: 1280px) and (max-width: 1535.98px) {
  .app-navbar__inner { gap: 0.75rem; }
  .app-navbar__link { padding: 0.5rem 0.5rem; letter-spacing: 0.02em; }
  .app-navbar__user { gap: 0.35rem; }
  .app-navbar__username { max-width: 90px; }
}
