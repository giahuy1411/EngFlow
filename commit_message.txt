refactor: dọn dẹp thực thể cũ và squash database migration V1

- **Dọn dẹp mã nguồn Java (Code Cleanup):**
  - Xóa bỏ hoàn toàn các thực thể cũ không dùng tới: Grammar, Exercise và ExerciseSubmission (đã được thay thế bởi cấu trúc LessonSection & LessonBlock động).
  - Loại bỏ các Service, Controller, Repository và Request/Response DTO liên quan đến Grammar/Exercise để dọn sạch code chết (dead code).
  - Cập nhật Lesson.java, LessonService.java và LessonResponse.java để loại bỏ các trường và logic ánh xạ của Grammar/Exercise.

- **Tối ưu hóa Database Migration (Flyway Squash):**
  - Gộp (Squash) toàn bộ các file migration từ V2 đến V5 vào duy nhất file `V1__init_schema.sql` để tạo bản thiết kế database gốc chuẩn chỉnh nhất.
  - Loại bỏ định nghĩa các bảng cũ (`grammar`, `exercises`, `exercise_submissions`) khỏi schema gốc.
  - Bổ sung định nghĩa các bảng cấu trúc mới (`lesson_skills`, `lesson_snapshots`, `lesson_sections`, `lesson_blocks`) cùng các index tối ưu hiệu năng vào V1.
  - Xóa bỏ các file migration thừa (V2, V3, V4, V5) để làm sạch thư mục `db/migration`.
